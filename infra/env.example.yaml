MONGODB_URI:
JWT_SECRET: "change_me"
NODE_ENV: "production"
CLIENT_URL: "https://gmat.site"
STRIPE_SECRET_KEY: 
STRIPE_WEBHOOK_SECRET: 
# Use either a pre-created recurring price OR provide product+amount+interval
# Preferred: pre-created recurring price in Stripe Dashboard
STRIPE_PRICE_ID: 
# Fallback: build price_data at runtime
STRIPE_PRODUCT_ID: 
STRIPE_PRICE_AMOUNT_CENTS: 
# one of: day | week | month | year
STRIPE_INTERVAL: 
# optional, defaults to usd
STRIPE_CURRENCY: usd
CLOUD_RUN_SERVICE_NAME: "gmat-practice-app"
SENDGRID_API_KEY: 
SENDGRID_FROM_EMAIL: "sale@gmat.site"
MONGODB_TLS_CERT: