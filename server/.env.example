# Server
PORT=8080
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/gmat-practice
# Optional X.509 auth (use either *_PATH to a file or inline PEMs below)
# MONGODB_TLS_CERT_PATH=
# MONGODB_TLS_CA_PATH=
# MONGODB_TLS_CERT=
# MONGODB_TLS_CA=

# Auth
JWT_SECRET=replace-with-a-strong-secret

# Client URL (for emails and redirects)
CLIENT_URL=http://localhost:3000

# Email Provider Selection
# Choose between 'sendgrid' or 'brevo' (default: sendgrid)
EMAIL_PROVIDER=sendgrid

# Email (SendGrid) - Option 1
# If SENDGRID_API_KEY is not set, emails will be logged to console in dev
# SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=no-reply@example.com

# Email (Brevo) - Option 2
# Brevo offers 300 free emails/day vs SendGrid's 100
# Get API key from: https://app.brevo.com/settings/keys/api
# BREVO_API_KEY=
# BREVO_FROM_EMAIL=no-reply@example.com
# BREVO_FROM_NAME=GMAT Practice

# Stripe (Subscriptions)
STRIPE_SECRET_KEY=sk_test_your_key
# Prefer STRIPE_PRICE_ID if you have a pre-created recurring price
# STRIPE_PRICE_ID=price_xxx
# Or use a product; default or first monthly price will be used
STRIPE_PRODUCT_ID=prod_xxx
STRIPE_CURRENCY=usd
STRIPE_INTERVAL=month
# Webhook secret for /api/payments/webhook verification
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=10
