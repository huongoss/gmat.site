# gcloud uses this to decide which files to upload to Cloud Build
# Start from scratch to ensure we only send what's needed

# VCS
.git
.gitignore
**/.DS_Store

# Local dependencies (installed inside build step)
node_modules
client/node_modules
server/node_modules

# Exclude mobile app from deployment source
mobile/
mobile/**

# Large/irrelevant artifacts
**/*.aab
**/*.apk
**/*.ipa
**/*.zip

# Credentials and secrets (never upload)
**/*.keystore
**/*.jks
**/*.pem
**/*.p12

# Cache
**/.cache
